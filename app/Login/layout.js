export const metadata = {
  title: "Information World - Login",
  description: "Access your Information World account. Log in to stay connected and manage your personalized settings and resources.",
};

export default function LoginLayout({ children }) {
  return (
    <div>
      
      {children}
    </div>
  );
}
